CC ?= cc
CFLAGS ?= -O2 -Wall -Wextra -std=c99
LDFLAGS ?=
LDLIBS ?= -lssl -lcrypto
WORKDIR ?= work

TARGET = $(WORKDIR)/spicebush
OBJS = $(WORKDIR)/spicebush_freebsd.o $(WORKDIR)/spicebush_shared.o $(WORKDIR)/spicebush_http_freebsd.o

all: $(TARGET)

$(WORKDIR):
	mkdir -p $(WORKDIR)

$(WORKDIR)/%.o: %.c | $(WORKDIR)
	$(CC) $(CFLAGS) -c $< -o $@

$(TARGET): $(OBJS)
	$(CC) $(LDFLAGS) -o $@ $(OBJS) $(LDLIBS)

clean:
	rm -f $(OBJS) $(TARGET)

.PHONY: all clean
